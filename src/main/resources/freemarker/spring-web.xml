<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:util="http://www.springframework.org/schema/util"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/mvc 
		http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/util
		http://www.springframework.org/schema/util/spring-util.xsd
		http://www.springframework.org/schema/aop 
		http://www.springframework.org/schema/aop/spring-aop-3.0.xsd">

	<!--配置springMVC -->
	<!--1:配置freemarker 显示ViewResolver -->
	<!-- 配置freemarker模板加载地址 -->
	<bean id="freemarkerConfigurer" class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
		<property name="templateLoaderPath" value="/WEB-INF/ftl/"/><!-- 模板文件路径 -->
		<property name="defaultEncoding" value="utf-8"/><!-- 字符集 -->
		<property name="freemarkerSettings" ref="freemarkerConfiguration" /><!-- 读取配置文件 -->
		<property name="freemarkerVariables">
			<map>
				<entry key="xml_escape" value-ref="fmXmlEscape" />
				<!-- 系统名称展示 -->
				<entry key="system_name" value="v粉卡管理系统" />
			</map>
		</property>
	</bean>

	<!-- 配置freemarker属性文件路径 -->
	<bean id="freemarkerConfiguration" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
		<property name="location" value="classpath:config/freemarker.properties"/>
	</bean>

	<!-- 配置freemarker视图解析器 -->
	<bean id="viewResolver" class="org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver">
		<property name="cache" value="true"/><!-- 开启模板缓存 -->
		<property name="suffix" value=".ftl"/><!-- 模板文件后缀 -->
		<property name="contentType" value="text/html;charset=utf-8"/>
		<property name="order" value="0" />
	</bean>

	<!-- 转义 -->
	<bean id="fmXmlEscape" class="freemarker.template.utility.XmlEscape"/>

	<!-- 文件上传 -->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="defaultEncoding" value="utf-8" />
		<property name="maxUploadSize" value="10485760000" />
		<property name="maxInMemorySize" value="40960" />
	</bean>

	<!--2:扫描web相关的bean -->
	<context:component-scan base-package="com.vivo.internet.vivoshop.vcard.admin" />

	<!--3:配置aop注解切面功能 -->
	<aop:aspectj-autoproxy proxy-target-class="true" />
	 <mvc:interceptors>
		 <mvc:interceptor>
			 <mvc:mapping path="/**"/>
             <bean class="com.vivo.internet.logsdk.web.LogSpringMvcInterceptor"/>
		 </mvc:interceptor>

		 <!-- 拦截器 -->
		 <mvc:interceptor>
			 <mvc:mapping path="/index"/>
			 <mvc:mapping path="/changePassword"/>
			 <mvc:mapping path="/user/**"/>
			 <mvc:mapping path="/member/**"/>
			 <mvc:mapping path="/order/**"/>
			 <mvc:mapping path="/cache/**"/>
			 <mvc:mapping path="/freeFlow/**"/>
			 <mvc:mapping path="/privilege/**"/>
			 <bean class="com.vivo.internet.vivoshop.vcard.admin.interceptor.LoginInterceptor"></bean>
		 </mvc:interceptor>
    </mvc:interceptors>
    <mvc:default-servlet-handler/>
</beans>